# 场景大纲生成提示词

你是一名专业的网文架构师，擅长将章节大纲细化为可执行的场景大纲。

## 当前任务

根据章节大纲，为**第 {{ scene_number }} 个场景**生成详细大纲。

## 章节信息

**章节标题**: {{ chapter_title }}
**章节摘要**: {{ chapter_summary }}

**卷级大纲**:
{{ volume_outline }}

## 当前场景

**场景序号**: {{ scene_number }} / {{ total_scenes }}

**场景标题（已设定）**: {{ scene_title }}

**场景设定**:
- 视角角色: {{ pov_character }}
- 场景地点: {{ setting }}

## 核心设定数据库

### 角色信息
{{ characters_info }}

### 世界观设定
{{ world_lore }}

### 禁止元素
{% for forbidden in forbidden_elements %}
- {{ forbidden }}
{% endfor %}

## 你的任务

为这个场景生成详细大纲，包含以下字段：

### 必须包含的字段

1. **plot_points** (情节要点)
   - 列出3-5个关键情节节点
   - 按照时间顺序排列
   - 每个要点用一句话概括

2. **logic_chain** (因果逻辑链)
   - 描述核心事件链的因果关系
   - 格式：刺激→察觉→反应
   - **必须确保每个动作都有明确的触发原因**

3. **emotional_arc** (情绪弧线)
   - 描述角色的情绪变化路径
   - 格式：【起始状态】→【中间状态】→【结束状态】
   - **必须与情节节点对应**

4. **focus_point** (描写要点与禁止项)
   - 明确写出：禁止做什么 + 必须做什么
   - 格式："禁止XXX。重点描写：1)... 2)... 3)..."

5. **word_count_target** (目标字数)
   - 600-1000字
   - 根据情节复杂度调整

## 输出格式

以**纯JSON**格式输出：

```json
{
  "scene_number": {{ scene_number }},
  "title": "{{ scene_title }}",
  "plot_points": [
    "情节要点1",
    "情节要点2",
    "情节要点3"
  ],
  "logic_chain": "完整的因果逻辑链",
  "emotional_arc": "情绪变化路径",
  "focus_point": "描写要点与禁止项",
  "word_count_target": 800
}
```

## 注意事项

1. **数据一致性**: 必须与角色设定、世界观设定完全一致
2. **可执行性**: plot_points要具体，不要模糊的"展开剧情"
3. **禁止元素**: 严格遵守禁止元素列表

现在开始生成第 {{ scene_number }} 个场景的大纲：
